import React from 'react';
import PropTypes from 'prop-types';

const Board = ({clonno}) => {
  console.log('Clonno: ', clonno);  
  return (
    <div>
      LOL
    </div>
  );
};

Board.propTypes = {
  clonno: PropTypes.any,
};

export default Board;
